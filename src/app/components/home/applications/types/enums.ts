export enum JobType {
  FullTime = "full-time",
  PartTime = "part-time",
  Contract = "contract",
  Internship = "internship",
}

export enum JobStatus {
  Applied = "applied",
  Interview = "interview",
  Accepted = "accepted",
  Rejected = "rejected",
  Ghosted = "ghosted",
}
