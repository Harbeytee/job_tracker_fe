import { IconNames } from "@/app/ui-library/types/types";

export interface ResumeCard {
  title: string;
  desc: string;
  iconName: IconNames;
  iconColour: string;
  button: string;
  link: string;
  iconBg: string;
}
